spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userAuthDb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: user-authentication-service
server:
  port: 8089
  
eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:8761/eureka
         
